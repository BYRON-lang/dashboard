rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all users to read and write to all documents
    // This is suitable for development and demo purposes
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
